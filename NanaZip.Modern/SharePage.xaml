<Page x:Class="NanaZip.Modern.SharePage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="using:NanaZip.Modern"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  IsTextScaleFactorEnabled="False"
  mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Border
      BorderBrush="{ThemeResource ContentDialogBorderBrush}"
      BorderThickness="{ThemeResource ContentDialogSeparatorThickness}"
      Padding="24,0,24,24"
      Grid.Row="0">
      <TextBlock
        x:Uid="/NanaZip.Modern/SharePage/TitleTextBlock"
        FontFamily="{StaticResource ContentControlThemeFontFamily}"
        FontSize="20"
        FontWeight="SemiBold"
        Foreground="{ThemeResource ContentDialogForeground}"
        Text="[How to send]" />
    </Border>
    <Border
      Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
      BorderBrush="{ThemeResource ContentDialogBorderBrush}"
      BorderThickness="{ThemeResource ContentDialogSeparatorThickness}"
      Grid.Row="1">
      <StackPanel
        HorizontalAlignment="Center"
        Orientation="Horizontal"
        Padding="{ThemeResource ContentDialogPadding}"
        Spacing="24">
        <StackPanel.Resources>
          <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
              <ResourceDictionary x:Key="Default">
                <x:String x:Key="CustomizeButtonIcon">&#x0020;</x:String>
                <FontFamily x:Key="SymbolThemeFontFamily">Segoe UI Emoji</FontFamily>
              </ResourceDictionary>
              <ResourceDictionary x:Key="HighContrast">
                <x:String x:Key="CustomizeButtonIcon">&#x0020;</x:String>
              </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            <Style TargetType="Button"
              BasedOn="{StaticResource DefaultButtonStyle}">
              <Setter Property="Background"
                Value="{ThemeResource AppBarButtonBackground}" />
              <Setter Property="BorderBrush"
                Value="{ThemeResource AppBarButtonBorderBrush}" />
              <Setter Property="HorizontalContentAlignment"
                Value="Center" />
              <Setter Property="VerticalContentAlignment"
                Value="Center" />
              <Setter Property="ToolTipService.Placement"
                Value="Mouse" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="{TemplateBinding CornerRadius}"
                      Height="{TemplateBinding Height}"
                      HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                      Padding="{TemplateBinding Padding}"
                      VerticalAlignment="{TemplateBinding VerticalAlignment}"
                      Width="{TemplateBinding Height}">
                      <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup x:Name="CommonStates">
                          <VisualState x:Name="Normal">
                            <VisualState.Setters>
                              <Setter Target="RootGrid.(RevealBrush.State)"
                                Value="Normal" />
                            </VisualState.Setters>
                            <Storyboard>
                              <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                            </Storyboard>
                          </VisualState>
                          <VisualState x:Name="PointerOver">
                            <VisualState.Setters>
                              <Setter Target="RootGrid.(RevealBrush.State)"
                                Value="PointerOver" />
                              <Setter Target="RootGrid.Background"
                                Value="{ThemeResource AppBarButtonBackgroundPointerOver}" />
                              <Setter Target="RootGrid.BorderBrush"
                                Value="{ThemeResource AppBarButtonBorderBrushPointerOver}" />
                              <Setter Target="ContentPresenter.Foreground"
                                Value="{ThemeResource ButtonForegroundPointerOver}" />
                            </VisualState.Setters>
                            <Storyboard>
                              <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                            </Storyboard>
                          </VisualState>
                          <VisualState x:Name="Pressed">
                            <VisualState.Setters>
                              <Setter Target="RootGrid.(RevealBrush.State)"
                                Value="Pressed" />
                              <Setter Target="RootGrid.Background"
                                Value="{ThemeResource AppBarButtonBackgroundPressed}" />
                              <Setter Target="RootGrid.BorderBrush"
                                Value="{ThemeResource AppBarButtonBorderBrushPressed}" />
                              <Setter Target="ContentPresenter.Foreground"
                                Value="{ThemeResource ButtonForegroundPressed}" />
                            </VisualState.Setters>
                            <Storyboard>
                              <PointerDownThemeAnimation Storyboard.TargetName="RootGrid" />
                            </Storyboard>
                          </VisualState>
                          <VisualState x:Name="Disabled">
                            <VisualState.Setters>
                              <Setter Target="RootGrid.Background"
                                Value="{ThemeResource AppBarButtonBackgroundDisabled}" />
                              <Setter Target="RootGrid.BorderBrush"
                                Value="{ThemeResource AppBarButtonBorderBrushDisabled}" />
                              <Setter Target="ContentPresenter.Foreground"
                                Value="{ThemeResource ButtonForegroundDisabled}" />
                            </VisualState.Setters>
                          </VisualState>
                        </VisualStateGroup>
                      </VisualStateManager.VisualStateGroups>
                      <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                      </Grid.RowDefinitions>
                      <FontIcon
                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                        FontSize="64"
                        Glyph="{ThemeResource CustomizeButtonIcon}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        Grid.Row="0" />
                      <ContentPresenter x:Name="ContentPresenter"
                        Content="{TemplateBinding Content}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        HorizontalContentAlignment="Center"
                        TextWrapping="WrapWholeWords"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                        VerticalContentAlignment="Center"
                        AutomationProperties.AccessibilityView="Raw"
                        Grid.Row="1" />
                    </Grid>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ResourceDictionary>
        </StackPanel.Resources>
        <Button x:Name="ShareButton"
          x:Uid="/NanaZip.Modern/SharePage/ShareButton"
          Content="[Share]"
          Height="128"
          HorizontalAlignment="Right"
          ToolTipService.ToolTip="[Share with other applications by Windows Share]">
          <Button.Resources>
            <ResourceDictionary>
              <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                  <x:String x:Key="CustomizeButtonIcon">&#x1F4E4;</x:String>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                  <x:String x:Key="CustomizeButtonIcon">&#xE72D;</x:String>
                </ResourceDictionary>
              </ResourceDictionary.ThemeDictionaries>
            </ResourceDictionary>
          </Button.Resources>
        </Button>
        <Button x:Name="EmailButton"
          x:Uid="/NanaZip.Modern/SharePage/EmailButton"
          Content="[Email]"
          Height="128"
          HorizontalAlignment="Right"
          ToolTipService.ToolTip="[Send an email by Windows Messaging]">
          <Button.Resources>
            <ResourceDictionary>
              <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Default">
                  <x:String x:Key="CustomizeButtonIcon">&#x1F4E8;</x:String>
                </ResourceDictionary>
                <ResourceDictionary x:Key="HighContrast">
                  <x:String x:Key="CustomizeButtonIcon">&#xE89C;</x:String>
                </ResourceDictionary>
              </ResourceDictionary.ThemeDictionaries>
            </ResourceDictionary>
          </Button.Resources>
        </Button>
      </StackPanel>
    </Border>
    <Border Grid.Row="2">
      <Border.Background>
        <AcrylicBrush
          BackgroundSource="HostBackdrop"
          FallbackColor="{ThemeResource SolidBackgroundFillColorSecondary}"
          TintColor="{ThemeResource SolidBackgroundFillColorSecondary}"
          TintLuminosityOpacity="0.96"
          TintOpacity="0.7" />
      </Border.Background>
      <StackPanel
        HorizontalAlignment="Right"
        Orientation="Horizontal"
        Padding="{ThemeResource ContentDialogPadding}"
        Spacing="8">
        <Button x:Name="CancelButton"
          x:Uid="/NanaZip.Modern/SharePage/CancelButton"
          Content="[Cancel]"
          HorizontalAlignment="Right"
          Style="{ThemeResource AccentButtonStyle}" />
      </StackPanel>
    </Border>
  </Grid>
</Page>